<div class='container'>
  <div class='row'>
    <div class='col-md-6 offset-md-3'>
      <h2><%= @trip.title %></h2>
      <%= form_with(model: @trip, url: schedules_update_public_trip_path(@trip)) do |f| %>
        <%= render 'public/trips/schedules_form', f: f %>
        <%= f.submit "一括更新" %>
      <% end %>
    </div>
  </div>
</div>